# Tempo-Bot (Combined History)

This repository merges three eras:
- legacy/v1 — earliest version (history only)
- legacy/v2 — mid-era refactor (history only)
- legacy/v3  current version (working tree)

Browse older eras via the era/v1 and era/v2 tags.
Sensitive files were removed from all history.
