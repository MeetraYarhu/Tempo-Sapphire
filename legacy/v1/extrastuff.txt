
       message.channel.send('Please enter a world name.').then(() => {
            message.channel.awaitMessages(filter, {
                    max: 1,
                    time: 30000,
                    errors: ['time'],
                })
                .then(collected => {
                    message.channel.send('Please enter an Aetheryte');
                })
                .catch(collected => {
                    message.channel.send('Request timed out.');
                });
        });
test change

conduct 12/28 ver

                const worldNames = ['levi', 'behe', 'lamia', 'famf', 'hype', 'exo', 'excal', 'ultros' ];


                            const worldlist = ['levi', 'leviathan', 'behe', 'behemoth', 'lamia', 'famf', 'famfrit', 'hype', 'hyperion', 'exo', 'exodus', 'excal', 'excalibur', 'ultros'];
            // m is the message being sent. index.js already does check for author.bot, so we omit here //
            const filter = m => m.author.id === message.author.id;
            message.reply('Please enter a world...');
            message.channel.awaitMessages(filter, {
                max: 1,
                time: 15000,
            }).then(collected => {
                    // Sets the world input to lower case for consistancy. //
                    const worldinput = (collected.first().content).toLowerCase();

                    // Worldname set to first three letters, to be used in future checks. //
                    let worldname = (collected.first().content).slice(0, 3);

                    // command will stop if 'cancel' is input //
                    if (worldinput === 'cancel') {
                        return message.reply('Canceled!');
                    }

                    if (worldlist.includes(`${worldinput}`)); {
                        return message.channel.send('Only standard shorthand and full names are valid inputs. Ex: "Levi" "Leviathan"');
                    };
                },
            }
}


            message.reply('Please enter a world...');
            // m is the message being sent. index.js already does check for author.bot, so we omit here //
            let filter = m => m.author.id === message.author.id;
            let collector = message.channel.createMessageCollector(filter, {
                time: 15000
            });




                            let counter = 0;

                const collector = new Discord.MessageCollector(message.channel, filter, {
                        max: questions.length,
                        time: 1000 * 15, // 15s
                });
                // const worldInput = (`${collector.collected.first()}`).toLowerCase();

                message.channel.send(questions[counter++]);
                collector.on('collect', m => {
                        if (counter < questions.length) {
                                m.channel.send(questions[counter++]);
                        }
                        for (let step = 0; step <= worldList.length - 1; step++) {
                                console.log(step);
                                // message.reply(step);
                                const worldTemp = worldList[step];
                                // if (worldList.includes(m.content)) { //
                                if (worldTemp === m.content) {
                                        {
                                                break;
                                        }
                                }
                        }
                });

                collector.on('end', collected => {
                        console.log(`Collected ${collected.size} messages.`);

                        if (collected.size < questions.length) {
                                message.reply('you ran out of time');
                        }

                        let temp = 0;
                        collected.forEach((value) => {
                                console.log(questions[temp++], value.content);

                                                // picture variables for aetheryte
                const tomraPng = 'https://i.imgur.com/2Z4u7Cf.png';
                const stilltideAetherytePng = 'https://i.imgur.com/UyOoTbw.png';
                const wrightPng = 'https://i.imgur.com/cJckhQu.png';
                const fortJobbPng = 'https://i.imgur.com/ACpbedg.png';
                const ostallImperativePng = 'https://i.imgur.com/qStoy8c.png';
                const wolekdorfPng = 'https://i.imgur.com/0SWCFFb.png';
                const plaEnniPng = 'https://i.imgur.com/qsaXQqM.png';
                const lydhaLranPng = 'https://i.imgur.com/SbbnWA3.png';
                const ondoCupsPng = 'https://i.imgur.com/SgM9hZY.png';
                const slitherboughPng = 'https://i.imgur.com/RseEN1N.png';
                const twinePng = 'https://i.imgur.com/jVUOj8Y.png';
                const fanowPng = 'https://i.imgur.com/kkHkq03.png';
                const crystariumPng = 'https://i.imgur.com/xKujf1O.png';
                const eulmorePng = 'https://i.imgur.com/ttIeRDC.png';
                const innPng = 'https://i.imgur.com/Q5e9FUx.png';
                const macarensesAnglePng = 'https://i.imgur.com/WdyPU5i.png';
                const mordSouqPng = 'https://i.imgur.com/jXb3cTp.png';

                        for (let i = 0; i <= locations.aetheryte.length - 1; i++) {
                                console.log(locations.aetheryte[i]['name']);
                                if (locations.aetheryte[i]['name'].toString() == locationName) {
                                        console.log(locations.aetheryte[i]);
                                        break;
                                } else { console.log('fail');}
                        }

                        locations.aetheryte.forEach(function(item)
                        {
                         newArray.push(item);
                        }); // newArray[0] = name newArray[1] = url


                                                let arrayCounter = 0;
                        aetheryteNames.push(locations.aetheryte[arrayCounter++]['name']);
                        if (arrayCounter < locations.aetheryte.length) {
                                aetheryteNames.push(locations.aetheryte[arrayCounter++]['name']);
                        }


                        {
    "aetheryte": [{
            "name": "Fort Jobb",
            "url": "https://i.imgur.com/ACpbedg.png"
        },
        {
            "name": "The Ostall Imperative",
            "url": "https://i.imgur.com/qStoy8c.png"
        },
        {
            "name": "Tomra",
            "url": "https://i.imgur.com/2Z4u7Cf.png"
        },
        {
            "name": "Stilltide",
            "url": "https://i.imgur.com/UyOoTbw.png"
        },
        {
            "name": "Wright",
            "url": "https://i.imgur.com/cJckhQu.png"
        },
        {
            "name": "Twine",
            "url": "https://i.imgur.com/jVUOj8Y.png"
        },
        {
            "name": "The Inn at Journey's Head",
            "url": "https://i.imgur.com/Q5e9FUx.png"
        },
        {
            "name": "Mord Souq",
            "url": "https://i.imgur.com/jXb3cTp.png"
        },
        {
            "name": "Wolekdorf",
            "url": "https://i.imgur.com/0SWCFFb.png"
        },
        {
            "name": "Lydha Lran",
            "url": "https://i.imgur.com/SbbnWA3.png"
        },
        {
            "name": "Pla Enni",
            "url": "https://i.imgur.com/qsaXQqM.png"
        },
        {
            "name": "Fanow",
            "url": "https://i.imgur.com/kkHkq03.png"
        },
        {
            "name": "Slitherbough",
            "url": "https://i.imgur.com/RseEN1N.png"
        },
        {
            "name": "The Ondo Cups",
            "url": "https://i.imgur.com/SgM9hZY.png"
        },
        {
            "name": "The Macarenses Angle",
            "url": "https://i.imgur.com/WdyPU5i.png"
        },
        {
            "name": "The Crystarium",
            "url": "https://i.imgur.com/xKujf1O.png"
        },
        {
            "name": "Eulmore",
            "url": "https://i.imgur.com/ttIeRDC.png"
        }
    ]
}

                        console.log(tempWorldNameSlice);
                        const tempWorldNameIndex = worlds.shorthand.indexOf(tempWorldNameSlice);
                        console.log(tempWorldNameIndex);
                        const relayWorld = worlds.Name[tempWorldNameIndex];


                                                // train speed variable checking for final embed relay
                        if (trainSpeed === 'na' || trainSpeed === 'none' || trainSpeed === 'unknown') {
                                relayEmbed.addFields({
                                        name: 'Speed',
                                        value: 'No set speed',
                                        inline: true,
                                });
                        } else if (trainSpeed === 'variable') {
                                relayEmbed.addFields({
                                        name: 'Speed',
                                        value: 'Variable',
                                        inline: true,
                                });
                        } else if (trainSpeed === 'fast' || trainSpeed === 'bullet') {
                                relayEmbed.addFields({
                                        name: 'Speed',
                                        value: 'Bullet',
                                        inline: true,
                                });
                        } else {
                                relayEmbed.addFields({
                                        name: 'Speed',
                                        value: 'Slow/Chill',
                                        inline: true,

                                });
                        }